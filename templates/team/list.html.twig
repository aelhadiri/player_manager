<h1>Hello {{ app.user }}!</h1>
<p>
    {% if 0 == teams|length %}
        {{ 'Level' | trans }}
    {%  elseif 1 ==  teams|length %}
        {{ 'Level' | trans }}
    {% else %}
        You have {{ teams|length }} teams.
    {% endif %}
</p>
<ul>
    {% for team in teams %}
        <li><a href="{{ path('app_team_show', {'slug': team.slug, 'level': team.level}) }}">{{ team.name }}</a> ({{ 'Level' | trans }} : {{ team.level }})</li>
    {% endfor %}
</ul>
<p>
        <a href="{{ path('app_team_list', {slug: club.slug}) }}">{{ 'Back to list of teams' | trans }}</a>
</p>